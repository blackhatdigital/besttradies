<% if current_page? root_path %>

<div class="row">
	<div class="large_header home">
  		<%= render 'layouts/homeheader' %>
	</div>
</div>

<% elsif current_page? jobs_path %>

<div class="row">
	<div class="large_header home">
  		<%= render 'layouts/tradyhomeheader' %>
	</div>
</div>

<% else %>

<div class="row">
	<div class="large_header home">
  		<%= render 'layouts/thinwhite' %>
	</div>
</div>

<% end %>